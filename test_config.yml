# Robot Framework Test Configuration
# Configure which tests to exclude during execution

# Test file to run
test_file: math_tests.robot

# Test names to skip (tests with these names will not run)
# Use exact test names as they appear in the .robot file
exclude_tests:
  - "Test Division - Should Fail"